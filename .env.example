# Base URL of the Symfony API. Leave empty in production builds served
# same-origin from the backend's public/app/ (relative /api/... calls work).
# In dev, point it at the Symfony dev server (php -S 127.0.0.1:8000 -t public).
VITE_API_BASE_URL=http://127.0.0.1:8000
